namespace InkjetOperator.Views;

/// <summary>
/// Setting page (UI only, blank placeholder). All controls, layout and design
/// properties live in InitializeComponent() inside the Designer file so the
/// Visual Studio WinForms Designer can open and edit this control.
/// No business logic, no runtime control creation, no custom painting.
/// </summary>
public partial class SettingUserControl : UserControl
{
    public SettingUserControl()
    {
        InitializeComponent();
    }
}
